name: iOS starter workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build and Test default scheme using any available iPhone simulator
    runs-on: macos-latest

    strategy:
      matrix:
        xcode: ["Xcode_16.4.app", "Xcode_26.3.app", "Xcode_26.0.app", "Xcode_26.1.1.app", "Xcode_26.2.app"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select xcode version
        run: |
          sudo xcode-select -s $XCODE_VERSION
        env:
          XCODE_VERSION: ${{ matrix.xcode }}

      - name: Log xcodebuild version
        run: |
          xcodebuild -version

      - name: Log available xcode versions
        run: |
          ls /Applications/ | grep "Xcode"

      - name: Run tests
        env:
          scheme: ${{ 'default' }}
          platform: ${{ 'iOS Simulator' }}

        run: |
          device=`xcrun xctrace list devices 2>&1 | grep -oE 'iPhone.*?[^\(]+' | head -1 | awk '{$1=$1;print}' | sed -e "s/ Simulator$//"`
          xcodebuild test -scheme "TestAppUITests" -project "TestApp.xcodeproj" -destination "platform=$platform,name=$device"
